{
  "name": "nexus-harness-qa",
  "version": "0.2.1",
  "private": true,
  "description": "Development-only browser QA dependencies for Nexus Harness.",
  "scripts": {
    "test": "python scripts/run_tests.py --quiet"
  },
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.62.1"
  }
}
